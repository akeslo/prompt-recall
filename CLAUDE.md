<protocol>
    <bootstrapping>CRITICAL: You are running in a manual, periodic session. Your FIRST action is always a System Audit. Scan the project root (package.json, go.mod, requirements.txt, .env, docker-compose.yml, etc.) to identify the current tech stack.</bootstrapping>
    <skill_indexing>Simultaneously, you MUST invoke `@using-superpowers` to index available agentic skills and verify the `SKILLS.md` registry. Do not proceed until context is grounded in these files. Make a note that all skills are globally defined.</skill_indexing>
    <context_hierarchy>Prioritize this file and the codebase state over general training data.</context_hierarchy>
</protocol>

<skill_orchestration>
    <planning>For tasks involving >2 files, use `@planning-with-files` for `task_plan.md`. Use `@loki-mode` for massive features.</planning>
    <debugging>Trigger `@systematic-debugging` before proposing fixes for reported bugs.</debugging>
    <ui_ux>Apply `@ui-ux-pro-max` and `@algorithmic-art` for frontend work.</ui_ux>
    <verification>Run `@lint-and-validate` and `@verification-before-completion`. Use `@playwright-skill` for visual confirmation.</verification>
    <security>Use `@vulnerability-scanner` focusing on `@top-web-vulnerabilities` for Auth/API/DB tasks.</security>
    <maintenance>Use `@production-code-audit` for legacy refactors and `@kaizen` for quality improvements.</maintenance>
</skill_orchestration>

<git_protocol>
    <atomic_logic>Work in small, logical units.</atomic_logic>
    <completion>Commit using Conventional Commits. Push to origin; create branch if missing.</completion>
</git_protocol>

<standards>
    <implementation>No placeholders (`// rest of code here`). Write full implementation.</implementation>
    <defensive>Assume inputs are malicious. Use strict type checks and `@backend-dev-guidelines`.</defensive>
    <dry>Follow `@clean-code` and `@senior-fullstack` patterns.</dry>
</standards>

<efficiency>
    <conciseness>No filler or apologies.</conciseness>
    <diffs>Prefer Diff-based editing for clarity.</diffs>
</efficiency>

<state_management>
    <hand_off>Provide 'Session Summary': Current Status, Discovered Tech/Skills, Active Bugs, and Next Actions.</hand_off>
</state_management>
